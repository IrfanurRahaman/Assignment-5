1️⃣ What is the difference between var, let, and const?

ans : - using var i can declare same variable multiple times without error
        also var can be used before declaration.

      - let variable allows to change the variable value 

      - using const type variable i cannot change the variable value  

2️⃣ What is the spread operator (...)?

ans : spread operator expands an array or an objects value individually

3️⃣ What is the difference between map(), filter(), and forEach()?

ans : - map() loop throughs an array and and create a new array with each element.

      - filter() gives new array containing only elements that ture with a condition.

      - forEach() loop through an array and brings out each element then starts a 
        condition.

4️⃣ What is an arrow function?

ans : using an arrow function allows to create a function in shorter syntax.

5️⃣ What are template literals?

ans : template literals (``) allows to write any variable,condition etc into a string.